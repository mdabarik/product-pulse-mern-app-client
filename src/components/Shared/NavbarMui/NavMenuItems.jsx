import Button from '@mui/material/Button';

const NavMenuItems = () => {
    return (
        <>
            <Button sx={{ color: '#fff' }}>
                {"Home"}
            </Button>
        </>
    );
};

export default NavMenuItems;